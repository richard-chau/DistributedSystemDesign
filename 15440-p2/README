My self designed proxy cache and remote server to handle concurrent clients requests.

Multiple clients can visit proxy, and multiple proxy can visit server.

All the file manipulations are implemented in one-copy semantics,
which means there is no conflict situation in the read/write process

Source Code: /sample